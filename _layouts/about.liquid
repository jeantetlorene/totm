---
layout: default
---
<div class="post">
  
  {% if page.top_image %}
    <div class="top-banner" style="background-image: url('{{ 'assets/img/' | append: page.top_image | relative_url }}');">
      <div class="top-banner-overlay">
        <div class="top-banner-content container">
          <h2 class="text-white" style="font-size: 2rem; margin-top: -10vh; margin-bottom: 2rem;">
            <em>«L’océan nous relie, veillons sur ses doyennes»</em>
          </h2>
          <h1 class="display-4 text-white font-weight-bold mb-3">TRANS OCÉANS TORTUES MARINES</h1>
          <p class="lead text-white">Les acteurs des territoires français pour la conservation des tortues marines à travers le monde</p>
        </div>
      </div>
    </div>
  {% endif %}


  <article>
    <div class="text-center my-5 px-3" style="max-width: 800px; margin-left: auto; margin-right: auto;">
      <p class="lead" style="font-size: 2.0rem;">
         {{ content }}
      </p>
    </div>

    {% if page.banners %}
      {% for banner in page.banners %}
      <section class="section {{ banner.background | default: 'bg-light' }} my-5 py-5">
        <div class="container">
          <div class="row align-items-center">
            {% if banner.image_position == "left" %}
            <!-- Image on Left -->
            <div class="col-lg-6 text-center mb-4 mb-lg-0">
              <img src="{{ 'assets/img/' | append: banner.image | relative_url }}" alt="banner image" class="img-fluid rounded-3" />
            </div>
            <div class="col-lg-6">
              <div class="px-lg-4 text-start">
                <h2 class="h2 mb-3">{{ banner.title }}</h2>
                <p class="mb-4">{{ banner.content }}</p>
                {% if banner.button.enable %}
                  <a href="{{ banner.button.link | relative_url }}" class="btn btn-outline-primary">{{ banner.button.label }}</a>
                {% endif %}
              </div>
            </div>
            {% else %}
            <!-- Image on Right -->
            <div class="col-lg-6 order-lg-2 text-center mb-4 mb-lg-0">
              <img src="{{ 'assets/img/' | append: banner.image | relative_url }}" alt="banner image" class="img-fluid rounded-3" />
            </div>
            <div class="col-lg-6 order-lg-1">
              <div class="px-lg-4 text-start">
                <h2 class="h2 mb-3">{{ banner.title }}</h2>
                <p class="mb-4">{{ banner.content }}</p>
                {% if banner.button.enable %}
                  <a href="{{ banner.button.link | relative_url }}" class="btn btn-outline-primary">{{ banner.button.label }}</a>
                {% endif %}
              </div>
            </div>
            {% endif %}
          </div>
        </div>
      </section>
      {% endfor %}
    {% endif %}


    <section class="container my-5">
      <link rel="stylesheet" href="{{ '/assets/css/gallery.css' | relative_url }}">
      <div class="inner">
        <h2>Gallery</h2>
        <p>This is a <strong>Gallery</strong> element. It no longer opens images in a lightbox—clicking now navigates to another page.</p>
      </div>
      <div class="gallery style2 medium lightbox onscroll-fade-in">
        {% for item in page.gallery %}
          <article>
            <a href="{{ item.url | relative_url }}" class="image">
              <img src="{{ 'assets/img/' | append: item.thumb | relative_url }}" alt="{{ item.title }}" />
            </a>
            <div class="caption">
              <h3>{{ item.title }}</h3>
              <p>{{ item.description }}</p>
            </div>
          </article>
        {% endfor %}
      </div>
      <!-- Custom Gallery Scripts -->
      <script src="{{ '/assets/js/jquery.min.js' | relative_url }}"></script>
      <script src="{{ '/assets/js/jquery.scrollex.min.js' | relative_url }}"></script>
      <script src="{{ '/assets/js/gallery.js' | relative_url }}"></script>
    </section>


    <div class="container my-5">
      <!-- Latest posts -->
      {% if page.latest_posts and page.latest_posts.enabled %}
        <h2 class="mt-5 mb-4">
          <a href="{{ '/blog/' | relative_url }}" style="color: inherit">Latest Posts</a>
        </h2>
        {% include latest_posts.liquid %}
      {% endif %}
    </div>



    <!-- Partenair logos -->
    {% if page.clients_logo_slider.enable %}
      <section class="section overflow-hidden py-6 my-5">
        <div class="container">
          <div class="row">
            <div class="col-12 text-center mb-5">
              <h2 class="h3">{{ page.clients_logo_slider.title }}</h2>
            </div>
            <div class="col-12">
              <div class="swiper brand-carousel">
                <div class="swiper-wrapper">
                  {% for logo in page.clients_logo_slider.logos %}
                    <div class="swiper-slide brand-item d-flex align-items-center justify-content-center">
                      <img src="{{ 'assets/img/' | append: logo | relative_url }}" alt="Brand logo" 
                            class="img-fluid" style="width: 200px; height: 100px;">
                    </div>
                  {% endfor %}
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>
    {% endif %}

    <!-- Social -->
    {% if page.social %}
      <div class="social">
        <div class="contact-icons">{% include social.liquid %}</div>

        <div class="contact-note">{{ site.contact_note }}</div>
      </div>
    {% endif %}

    {% if site.newsletter and site.newsletter.enabled and site.footer_fixed %}
      {% include newsletter.liquid center=true %}
    {% endif %}
  </article>



</div>
