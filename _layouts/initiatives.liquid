---
layout: page
---

<div class="post">
    
    
    <article>
        {{ content }}
    </article>

    <a href="{{ page.pdf | prepend: 'assets/pdf/' | relative_url }}" class="btn btn-outline-primary" target="_blank" rel="noopener noreferrer">
        Télécharger le Programme Initiatives TOTM
    </a>

    <section class="container my-5">
        <div class="row">
            {% for item in page.gallery %}
                <div class="col-md-4 mb-4">
                    <div class="card h-100 border-0 shadow-sm">
                    <a href="{{ item.url | relative_url }}"class="position-relative d-block">
                        <img src="{{ 'assets/img/' | append: item.thumb | relative_url }}" class="card-img-top" alt="{{ item.title }}">
                        <div class="card-img-overlay d-flex align-items-center justify-content-center p-0">
                        <div class="w-100 text-center bg-dark bg-opacity-50 text-white py-2">
                            <h5  class="mb-0" style="color: white;">{{ item.title }}</h5>
                        </div>
                        </div>
                    </a>
                    </div>
                </div>
            {% endfor %}
        </div>
    </section>
</div>